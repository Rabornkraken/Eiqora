# Config

Store data-collection configuration here (env templates, connection strings,
source credentials). Keep secrets out of Git and use separate env files for
config vs. API keys.

Template:
- `data_collection/config/.env` (all settings + secrets)
